# Job completion triggers are managed inline on dbtcloud_job via the
# job_completion_trigger_condition attribute (which is in ignore_changes).
# The provider does not expose a separate dbtcloud_job_completion_trigger resource.
