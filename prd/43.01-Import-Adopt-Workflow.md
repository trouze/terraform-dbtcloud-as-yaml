# PRD: Adoption Workflow & Import

**Status:** Part 1 — Ready for Implementation | Part 2 — Future  
**Created:** 2026-02-11  
**Version:** 0.21.0+

---

## Context

Adoption is the process of bringing **existing dbt Cloud infrastructure** under Terraform management. This applies in two scenarios:

1. **Migration Adoption (Part 1)** — Within the Migration workflow, users adopt target resources into Terraform management. This covers two sub-scenarios:
   - **Source-matched adoption:** A source resource is matched to an existing target resource. The target's IDs and configuration are used so Terraform manages the existing resource without recreating it.
   - **Target-only adoption:** A target resource exists with no corresponding source resource. The user can still choose to bring it under Terraform management (e.g., the target account has projects D and E that don't exist in the source).

2. **Import & Adopt (Part 2)** — A future standalone workflow that works purely against a single target account (no source). Users select which target resources to bring under Terraform management.

Part 1 is the immediate focus. All components built for Part 1 should be designed for reuse by Part 2. The target-only adoption sub-scenario in Part 1 directly exercises the same code paths that Part 2 will use exclusively.

### Existing Codebase Stubs

- `WorkflowType.IMPORT_ADOPT` in `importer/web/state.py` (line 45)
- `WORKFLOW_STEPS[IMPORT_ADOPT]` currently mirrors Migration steps (includes source-side steps — will need updating for Part 2)
- Disabled in the UI: "Coming Soon" badge, `disabled=True` on `home.py` (lines 338–346)
- Static label "Import & Adopt (Coming Soon)" below the workflow dropdown in `stepper.py` (lines 99–103)
- No dedicated pages or logic exist yet for the standalone workflow

---

## Part 1: Migration Adoption (Immediate)

### Goal

Ensure the Migration workflow's adoption capabilities are complete, well-tested, and built with reusable abstractions so the future Import & Adopt workflow can reuse them.

### Non-Goals

- Implementing the standalone Import & Adopt workflow (Part 2)
- Changing the Migration workflow's existing UX or step order
- Adding new workflow steps or pages to Migration
- Global resource matching (see [42.01 Part 2](42.01-Global-Resources-Target-Intent.md))

---

### Existing Infrastructure

These components already exist and form the foundation for Part 1 user stories:

| Component | File | Key Functions |
|-----------|------|---------------|
| Import block generator | `importer/web/utils/terraform_import.py` | `generate_import_blocks()`, `generate_adopt_imports_from_grid()`, `write_adopt_imports_file()`, `supports_import_blocks()` (TF 1.5+ detection), legacy CLI fallback |
| YAML adoption overrides | `importer/web/utils/adoption_yaml_updater.py` | `apply_adoption_overrides()` — updates YAML with target values for adopted resources |
| Target normalization | `importer/web/utils/target_intent.py` | `normalize_target_fetch()` — generates baseline YAML from target fetch data; `compute_target_intent()` — builds target intent from dispositions |
| Protection intent | `importer/web/utils/protection_intent.py` | `ProtectionIntentManager` — tracks protection status per resource |
| Protection utilities | `importer/web/utils/protection_manager.py` | Cascade protect/unprotect, moved blocks, mismatch detection, `RESOURCE_TYPE_MAP`, `EXTENDED_RESOURCE_TYPE_MAP` |
| Match grid | `importer/web/components/match_grid.py` | `build_grid_data()` — builds AG Grid rows from source/target data |
| Hierarchy index | `importer/web/components/hierarchy_index.py` | `ENTITY_PARENT_TYPES`, `TYPE_DEPTH`, `TYPE_SORT_ORDER` — parent/child relationships for cascade |
| TF state reader | `importer/web/utils/terraform_state_reader.py` | `TF_TYPE_TO_CODE`, `CODE_TO_TF_TYPE` — maps TF resource types to internal codes |

### Output Files

Import block generation produces these files in the deployment directory:

| File | Context | Generator |
|------|---------|-----------|
| `imports.tf` | Deploy page — confirmed mappings | `write_import_blocks_file()` |
| `adopt_imports.tf` | Match page — adopt grid rows | `write_adopt_imports_file()` |
| `reconcile_imports.tf` | Drift reconciliation | `write_reconcile_import_blocks_file()` |

---

### User Stories

#### 1. Match Grid Adopt Flow (Source-Matched)

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-MA-01 | As a migration operator, I want to see an "adopt" action option for resources that have a matching target resource | When a source resource matches a target resource, the action dropdown includes "Adopt Existing" | yes |
| US-MA-02 | As a migration operator, I want to see the target resource ID and name when adopting | Grid row shows target resource name, ID, and project when action is "adopt" | yes |
| US-MA-03 | As a migration operator, I want to bulk-adopt all matched resources | "Adopt All Matched" button sets action to "adopt" for all rows with a target match | yes |
| US-MA-04 | As a migration operator, I want to bulk-ignore unmatched resources | "Ignore All Unmatched" button sets action to "ignore" for rows without target matches | yes |
| US-MA-05 | As a migration operator, I want to see a visual indicator for adopted resources in the grid | Adopted rows show a distinct badge/icon (e.g., "Adopted" badge) differentiating them from "Create New" | yes |
| US-MA-06a | As a migration operator, I want to see a "Select Whole Project" dialog with child resource counts | "Select Whole Project" action opens a dialog listing: "Adopt all resources in Project X? N environments, M jobs, K repositories, ..." | yes |
| US-MA-06b | As a migration operator, I want "Adopt All" in the project dialog to adopt every child resource | Clicking "Adopt All" in the dialog sets action to "adopt" for the project and all its children | yes |
| US-MA-06c | As a migration operator, I want to customize which children are adopted via checkboxes in the project dialog | Dialog shows per-type checkboxes; unchecking a type sets those children to "ignore" while checked types are set to "adopt" | yes |
| US-MA-07 | As a migration operator, I want to cherry-pick individual resources for adoption | Individual row action dropdown allows changing any single resource to "adopt" or "ignore" regardless of project-level selection | yes |
| US-MA-08 | As a migration operator, I want to see a summary of adoption decisions before proceeding | Summary card shows: N resources to adopt (matched), P to adopt (target-only), M to create new, K to ignore, J already managed | yes |

#### 1a. Match Scope Visibility Filter

The Match grid currently shows state-only rows (resources in TF state with no source counterpart) regardless of source scope. With target-only adoption (section 1b), target-only rows will also appear. A scope visibility filter lets users focus the grid on what they selected in the Scope step.

**This is a pure visibility filter.** It does NOT affect intent computation, protection logic, import block generation, YAML output, or any Terraform operations. Hidden rows retain their current action/state — they are just not displayed.

- **Filter OFF (full view, default):** All rows visible — source-scoped items, state-only items, target-only items.
- **Filter ON (scoped view):** Only rows connected to source-scoped items are visible. State-only and target-only rows that have no relationship to any scoped source item are hidden.

A row is "connected to a scoped source" if:
- It IS a source item in the scope selection (matched or unmatched)
- It is a target/state resource that is matched to a scoped source item

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-SV-01 | As a migration operator, I want a "Show Scoped Only" toggle in the Match grid toolbar | Toggle appears alongside the existing type filter; default is OFF (full view) | yes |
| US-SV-02 | As a migration operator, I want the scoped view to show only source-scoped rows and their matches | When toggle is ON, only rows whose source item is in the SelectionManager's selected_ids appear; their matched target data is included | yes |
| US-SV-03 | As a migration operator, I want state-only rows hidden when scoped view is active | State-only rows (`is_state_only: True`) are hidden when toggle is ON, since they have no scoped source counterpart | yes |
| US-SV-04 | As a migration operator, I want target-only rows hidden when scoped view is active | Target-only rows (from section 1b) are hidden when toggle is ON, since they have no scoped source counterpart | yes |
| US-SV-05 | As a migration operator, I want the scoped view to be purely visual — no impact on other logic | Toggling the filter does not change any row's action, disposition, protection status, or intent; hidden rows retain their state | no |
| US-SV-06 | As a migration operator, I want the summary card to update to reflect visible rows when filtered | Summary counts (adopt, create, ignore, already managed) update to show counts for visible rows only, with a note "(filtered — N rows hidden)" | yes |
| US-SV-07 | As a migration operator, I want the scope filter to work alongside the existing type filter | Both filters can be active simultaneously; a row must pass both filters to be visible | yes |



Target resources that exist in the target account but have **no corresponding source resource** should also be surfaceable and adoptable within the Migration workflow. These rows appear in the same Match grid, visually distinguished from source-matched rows.

**Important:** The ability to match a source resource to an existing target resource (i.e., selecting "Adopt Existing" on a source-driven row) is **always available** regardless of any target-only preference. The preference below only controls the visibility of target-only rows — resources that have no source equivalent at all.

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-TO-01 | As a migration operator, I want to see target resources that have no source match in the Match grid | Target-only resources appear in the grid with an empty Source column and a "Target Only" badge | yes |
| US-TO-02 | As a migration operator, I want target-only resources to default to "Ignore" | Target-only rows have action="ignore" by default; user explicitly opts in to adopt them | no |
| US-TO-03 | As a migration operator, I want to change a target-only resource's action to "Adopt" | Action dropdown for target-only rows includes "Adopt" and "Ignore" (no "Create New" — the resource already exists) | yes |
| US-TO-04 | As a migration operator, I want a toggle to show/hide target-only resources | "Show Target-Only Resources" toggle in the grid toolbar filters target-only rows in/out; default state is driven by preference (US-TP-01) | yes |
| US-TO-05 | As a migration operator, I want to bulk-adopt all target-only resources | "Adopt All Target-Only" button sets action to "adopt" for all target-only rows | yes |
| US-TO-06 | As a migration operator, I want YAML generated for target-only adopted resources using target data | Target-only adoptions generate YAML via `normalize_target_fetch()` (no source YAML to merge — purely target-derived configuration) | no |
| US-TO-07 | As a migration operator, I want import blocks generated for target-only adopted resources | Target-only adoptions produce `import {}` blocks in `adopt_imports.tf` just like source-matched adoptions | no |
| US-TO-08 | As a migration operator, I want target-only adoptions to support protection | Target-only adopted resources can be marked as protected with the same checkbox/cascade behavior as source-matched adoptions | yes |
| US-TO-09 | As a migration operator, I want target-only adoptions to participate in dependency cascades | Adopting a target-only job triggers the same dependency dialog (environment, project) — dependencies can be either source-matched or target-only | yes |
| US-TO-10 | As a migration operator, I want target-only resources grouped by project in the grid | Target-only resources appear under their project grouping alongside source-matched resources from the same project | yes |

#### 1c. Target-Only Adoption Preference

Not all migration operators want to be presented with target-only resources. A two-level preference controls the default visibility:

- **Project-level default** — persists across sessions (stored in project config / `.env`). Once set, the Match page respects it without re-asking.
- **Per-session override** — the "Show Target-Only Resources" toggle in the grid toolbar overrides the project default for the current session.

A smart first-run dialog appears **only when unmatched target resources are detected** (target resources that don't match any source resource and aren't already in TF state). If all target resources are either matched or in state, the dialog does not appear.

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-TP-01 | As a migration operator, I want a project-level preference for whether target-only resources are shown by default | Project config stores `show_target_only_resources: true/false`; the grid toggle defaults to this value | no |
| US-TP-02 | As a migration operator, I want a first-run dialog when unmatched target resources exist | On first Match page load, if there are target resources with no source match and no TF state entry, a dialog appears: "We found N target resources that don't match any source resources. Would you like to see them for potential adoption?" | yes |
| US-TP-03 | As a migration operator, I want to dismiss the dialog with "Yes, show them" | Dialog sets toggle to ON; preference saved to session state; target-only rows visible | yes |
| US-TP-04 | As a migration operator, I want to dismiss the dialog with "No thanks, just focus on migration" | Dialog sets toggle to OFF; preference saved to session state; target-only rows hidden | yes |
| US-TP-05 | As a migration operator, I want a "Remember this choice" option in the dialog | Checkbox in the dialog saves the choice as the project-level default; dialog does not appear again for this project | yes |
| US-TP-06 | As a migration operator, I want the dialog to NOT appear when all target resources are already matched or in state | If no unmatched target-only resources exist, the dialog is skipped entirely regardless of preference state | yes |
| US-TP-07 | As a migration operator, I want to change the project-level preference later | Configure page includes a "Target-Only Adoption" toggle to change the project-level default at any time | yes |
| US-TP-08 | As a migration operator, I want to override the preference for a single session via the grid toggle | The grid toolbar toggle always works regardless of project-level preference; changing it does not update the project default | yes |
| US-TP-09 | As a migration operator, I want source-to-target matching to always be available regardless of this preference | Setting the preference to "No" only hides target-only rows; the ability to select an existing target resource when matching a source resource is never affected | no |

#### 2. YAML Generation

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-YG-01 | As a migration operator, I want adopted resources to use target values in the generated YAML | `dbt-cloud-config.yml` contains target resource IDs and names for adopted resources via `apply_adoption_overrides()` | no |
| US-YG-02 | As a migration operator, I want the YAML to merge source configuration with target adoption overrides | Source YAML structure is preserved; only ID fields and matched names are overridden with target values | no |
| US-YG-03 | As a migration operator, I want `protected: true` set in YAML for resources I marked as protected | Adopted resources with protection enabled have `protected: true` in their YAML entry | no |
| US-YG-04 | As a migration operator, I want YAML regeneration to preserve my adoption and protection decisions | Regenerating YAML from the Match page retains previously confirmed adoption and protection state | no |
| US-YG-05 | As a migration operator, I want the YAML to be valid against the v2 schema after adoption overrides | Generated YAML passes `schemas/v2.json` validation including `protected` field | no |

#### 3. Import Blocks

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-IB-01 | As a migration operator, I want TF import blocks generated for all adopted resources | `adopt_imports.tf` contains `import { to = ...; id = "..." }` blocks for every resource with action="adopt" | no |
| US-IB-02 | As a migration operator, I want import blocks to use correct Terraform resource addresses | Import block `to` addresses match the module structure: `module.dbt_cloud.<tf_type>.<block_name>["<key>"]` | no |
| US-IB-03 | As a migration operator, I want import blocks to reference the correct target resource IDs | Import block `id` values match the target dbt Cloud resource IDs | no |
| US-IB-04 | As a migration operator, I want protected resources to use protected block addresses in import blocks | Protected adopted resources use `protected_<type>` block names (e.g., `protected_jobs` instead of `jobs`) | no |
| US-IB-05 | As a migration operator, I want to preview import blocks before they are written | "Preview" button shows the generated import block content before writing to disk | yes |
| US-IB-06 | As a migration operator, I want the system to detect TF version and use appropriate import method | TF 1.5+ uses `import {}` blocks; older versions fall back to `terraform import` CLI commands | no |
| US-IB-07a | As a migration operator, I want import blocks generated for project and environment types | `generate_adopt_imports_from_grid()` produces correct `import {}` blocks for PRJ and ENV resource types | no |
| US-IB-07b | As a migration operator, I want import blocks generated for job and repository types | `generate_adopt_imports_from_grid()` produces correct `import {}` blocks for JOB, REP, and PREP resource types | no |
| US-IB-07c | As a migration operator, I want import blocks generated for env variables, extended attributes, and credentials | `generate_adopt_imports_from_grid()` produces correct `import {}` blocks for VAR, EXTATTR, and credential resource types | no |

#### 4. TF State Awareness

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-TS-01 | As a migration operator, I want to see which target resources are already in the Terraform state | Resources present in `terraform.tfstate` show "Already Managed" status in the grid | yes |
| US-TS-02 | As a migration operator, I want already-managed resources to be auto-included (not re-imported) | Resources in TF state are automatically included in the intent but excluded from import block generation | no |
| US-TS-03 | As a migration operator, I want a warning when an adopted resource is already in TF state | If a user tries to adopt a resource already in state, a warning indicates it's already managed and import would be skipped | yes |
| US-TS-04 | As a migration operator, I want to see the TF state address for already-managed resources | Grid shows the Terraform address (e.g., `module.dbt_cloud.dbtcloud_job.jobs["my_job"]`) for state-present resources | yes |
| US-TS-05 | As a migration operator, I want state cross-referencing to work even when protection status differs | A resource in state as `protected_jobs["x"]` is recognized even when the current intent has it unprotected (or vice versa) | no |

#### 5. Protection

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-PR-01 | As a migration operator, I want to mark adopted resources as protected during the match step | Protection checkbox/column is available in the match grid for resources with action="adopt" | yes |
| US-PR-02 | As a migration operator, I want protecting a child resource to suggest protecting its parents | Protecting a job shows a cascade dialog listing the environment and project that should also be protected (per 31.02 pattern) | yes |
| US-PR-03 | As a migration operator, I want to skip cascade protection if I choose to | Cascade dialog has "Skip Dependencies" option that protects only the selected resource | yes |
| US-PR-04 | As a migration operator, I want protected adopted resources to generate `lifecycle { prevent_destroy = true }` | Protected resources are placed in `protected_<type>` TF resource blocks with `prevent_destroy = true` | no |
| US-PR-05 | As a migration operator, I want protection decisions to persist in the protection intent file | `ProtectionIntentManager` saves protection state to disk and reloads it across page navigations | no |
| US-PR-06 | As a migration operator, I want `moved` blocks generated when I change a resource's protection status | Changing `protected: true` to `false` (or vice versa) generates a `moved {}` block so Terraform moves the resource without recreating it | no |

#### 6. Deploy Integration

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-DI-01a | As a migration operator, I want `terraform plan` to show the count of resources that will be imported | `terraform plan` output summary includes the count of resources "to import" | yes |
| US-DI-01b | As a migration operator, I want `terraform plan` to show adopted resources as imports, not creates | `terraform plan` output shows "will be imported" for each adopted resource, not "will be created" | yes |
| US-DI-02a | As a migration operator, I want `terraform apply` to import adopted resources into state | After apply, `terraform.tfstate` contains the adopted resources with their target IDs | yes |
| US-DI-02b | As a migration operator, I want to verify the state file contains the correct target IDs after apply | Each imported resource in `terraform.tfstate` references the correct dbt Cloud target resource ID | no |
| US-DI-03 | As a migration operator, I want clear error messages when an import fails | If a resource import fails (e.g., wrong ID, resource deleted), the error message identifies which resource and suggests corrective action | yes |
| US-DI-04 | As a migration operator, I want protection warnings shown before plan/apply | If plan would destroy protected resources, a warning is shown before the Terraform output | yes |
| US-DI-05 | As a migration operator, I want to see the count of resources to import in the deploy summary | Deploy page summary shows: "N resources to import, M to create, K protected" | yes |
| US-DI-06 | As a migration operator, I want import blocks cleaned up after successful apply | After successful `terraform apply`, the `adopt_imports.tf` file can be removed (imports are now in state) | no |

#### 7. Dependency Handling

| ID | Story | Acceptance Criteria | BV |
|----|-------|---------------------|----|
| US-DH-01 | As a migration operator, I want a warning when I adopt a child resource without its parent | Adopting a job without its environment/project triggers a cascade dialog: "To adopt Job X, these resources should also be adopted: [Production env, Analytics project]" | yes |
| US-DH-02 | As a migration operator, I want the option to auto-adopt required dependencies | Cascade dialog has "Adopt All" button that adopts the child and all required parents | yes |
| US-DH-03 | As a migration operator, I want the option to skip dependency adoption | Cascade dialog has "Skip Dependencies" button — the user proceeds without adopting parents (may cause TF errors) | yes |
| US-DH-04 | As a migration operator, I want dependency resolution to use the existing hierarchy index | Dependency lookups use `hierarchy_index.py` `ENTITY_PARENT_TYPES` for parent resolution (same as cascade protection) | no |
| US-DH-05 | As a migration operator, I want "Select Whole Project" to include all child resource types | Selecting a project for adoption includes environments, jobs, repositories, env variables, extended attributes, and credentials within that project | yes |
| US-DH-06 | As a migration operator, I want to customize which children are included after "Select Whole Project" | After "Select Whole Project", a dialog shows all child resources grouped by type with checkboxes to include/exclude individual items | yes |

---

### Architectural Decisions

| ID | Decision | Rationale |
|----|----------|-----------|
| AD-1 | **Project granularity: Hybrid approach** — Grid organized by project for viewing; "Select Whole Project" dialog with child counts and customization; cherry-pick individual resources | Balances ease of use (bulk project adoption) with fine-grained control (individual resource selection) |
| AD-2 | **Dependency handling: Dialog assist, non-blocking** — Cascade-style dialog suggests adopting dependencies; user can skip | Matches 31.02 protection cascade pattern; non-blocking respects user autonomy while warning about risks |
| AD-3 | **Intent format: Reuse target-intent.json** — Add `adopt` and `ignore` dispositions alongside existing `match`/`create` | Single intent file simplifies state management; disposition types are workflow-aware |
| AD-4 | **Import block format: TF 1.5+ import {} blocks** — Declarative blocks in config files | Declarative approach is plannable/reviewable; TF 1.5 is 3+ years old; legacy CLI fallback already exists in `terraform_import.py` |
| AD-5 | **Target-only display: Same grid, visually distinguished** — Target-only resources appear in the Match grid alongside source-matched rows with a "Target Only" badge, empty Source column, and filter toggle | Dependencies cross the boundary (target-only job may depend on source-matched project); single grid enables cascade dialogs; filter toggle lets users focus; directly exercises Part 2 code paths |
| AD-6 | **Target-only preference: Two-level opt-in** — Project-level default (persists across sessions) + per-session toggle override; smart first-run dialog only when unmatched target resources detected | Respects users who just want to migrate without noise; smart trigger avoids unnecessary dialogs; project-level persistence prevents repeated asking; source-to-target matching always available regardless |
| AD-7 | **Match scope visibility filter: Pure UI filter** — "Show Scoped Only" toggle hides state-only and target-only rows not connected to source scope; does not affect intent, protection, imports, or any TF logic | Keeps grid focused for users migrating specific projects; composable with type filter and target-only preference; hidden rows retain their state |

---

### Files to Change (Part 1)

| File | Action | Notes |
|------|--------|-------|
| `importer/web/pages/match.py` | Modify | Adopt action flow, dependency cascade dialogs, project-level selection, adoption summary |
| `importer/web/components/match_grid.py` | Modify | Adopt indicators, bulk adopt/ignore actions, project grouping controls |
| `importer/web/utils/terraform_import.py` | Modify | Extend `generate_adopt_imports_from_grid()` for all resource types, protected address handling |
| `importer/web/utils/adoption_yaml_updater.py` | Modify | Ensure all resource types covered, protection flag handling |
| `importer/web/utils/target_intent.py` | Modify | Add `adopt`/`ignore` dispositions to intent format, workflow-type-aware validation |
| `importer/web/utils/protection_intent.py` | Modify | Integration with adopt flow for protection during adoption |
| `importer/web/utils/terraform_state_reader.py` | Modify | State cross-referencing for already-managed detection |
| `importer/web/pages/deploy.py` | Modify | Import count in summary, import block cleanup after apply |
| `importer/web/components/hierarchy_index.py` | Reference | Used for dependency resolution (parent lookups); no changes expected |

---

### Test Plan

#### Unit Tests

| Test ID | Test Case | Expected Result |
|---------|-----------|-----------------|
| UT-AD-01 | `generate_adopt_imports_from_grid()` with mixed adopt/ignore rows | Import blocks generated only for adopt rows |
| UT-AD-02 | `generate_adopt_imports_from_grid()` with protected adopt rows | Import blocks use `protected_<type>` addresses |
| UT-AD-03 | `apply_adoption_overrides()` with target values | YAML contains target IDs and names for adopted resources |
| UT-AD-04 | `apply_adoption_overrides()` preserves `protected: true` | Protection flag survives YAML override |
| UT-AD-05 | Import block addresses for all resource types (PRJ, ENV, JOB, REP, PREP, EXTATTR, VAR) | Each type resolves to correct `module.dbt_cloud.<tf_type>.<block>["key"]` address |
| UT-AD-06 | State cross-reference identifies already-managed resources | Resources in TF state return "already_managed" status |
| UT-AD-07 | State cross-reference handles protection address mismatch | Resource in state as `protected_jobs["x"]` matches intent with `jobs["x"]` |
| UT-AD-08 | Dependency resolution for job → environment → project chain | `ENTITY_PARENT_TYPES` correctly returns [ENV, PRJ] for JOB |
| UT-AD-09 | `target-intent.json` serialization with `adopt`/`ignore` dispositions | Round-trip: serialize → deserialize preserves disposition types |
| UT-AD-10 | `supports_import_blocks()` version detection | TF 1.5.0+ returns True; older returns False |
| UT-AD-11 | `normalize_target_fetch()` generates valid YAML for target-only resources | Target resources with no source equivalent produce valid YAML entries |
| UT-AD-12 | `generate_adopt_imports_from_grid()` handles mixed source-matched and target-only rows | Import blocks generated for both row types; addresses and IDs correct |
| UT-AD-13 | Target-only rows excluded when filter toggle is off | Grid filtering respects "Show Target-Only Resources" toggle state |
| UT-AD-14 | Project-level preference loads correctly on Match page | Toggle default matches `show_target_only_resources` project config |
| UT-AD-15 | First-run dialog triggers only when unmatched target-only resources exist | Dialog does not appear when all targets are matched or in state |
| UT-AD-16 | Scope visibility filter hides state-only rows | With filter ON, rows with `is_state_only: True` are hidden |
| UT-AD-17 | Scope visibility filter hides target-only rows | With filter ON, target-only rows (no source counterpart) are hidden |
| UT-AD-18 | Scope visibility filter preserves source-scoped rows and their matches | With filter ON, source-scoped rows and their matched target data remain visible |
| UT-AD-19 | Scope visibility filter does not alter row actions or dispositions | Toggling the filter does not change any row's action, protection, or intent state |
| UT-AD-20 | `build_grid_data()` produces `is_target_only: True` flag for target-only resources | Rows for target resources with no source match have `is_target_only` set to `True` |
| UT-AD-21 | `build_grid_data()` target-only rows have default action "ignore" | Target-only rows are initialized with `action="ignore"` (not "adopt" or "create") |
| UT-AD-22 | `build_grid_data()` target-only rows have empty source columns | Target-only row dicts have `source_name`, `source_id`, and `source_project` as empty/null |
| UT-AD-23 | Adoption summary count logic: correct counts per action type | Given a grid with mixed adopt/create/ignore/already-managed rows, summary returns correct count per action |
| UT-AD-24 | "Select Whole Project" child enumeration returns all child types | Given a project row, child enumeration returns all ENV, JOB, REP, PREP, EXTATTR, VAR children in that project |
| UT-AD-25 | Negative: `generate_adopt_imports_from_grid()` with zero adopt rows produces empty output | When all rows are "ignore" or "create", import block output is empty |
| UT-AD-26 | Negative: adoption override with missing target ID raises/handles gracefully | `apply_adoption_overrides()` with a row missing `target_id` raises a clear error or skips gracefully |
| UT-AD-27 | `build_grid_data()` mixed source-matched + target-only + state-only produces correct flags on each row type | Grid with all three row types has `is_target_only`, `is_state_only`, and matched flags set correctly per row |

#### Integration Tests

| Test ID | Test Case | Expected Result |
|---------|-----------|-----------------|
| IT-AD-01 | Full adoption flow: match → adopt → generate YAML → generate import blocks | All output files valid; YAML passes v2 schema; import blocks reference correct addresses |
| IT-AD-02 | Adoption with TF state present (some resources already managed) | Already-managed resources excluded from import blocks; new adopts included |
| IT-AD-03 | Protected adoption: adopt with `protected: true` → import blocks + moved blocks | Import blocks use protected addresses; moved blocks generated if needed |
| IT-AD-04 | Project-level adoption with child customization | Adopting project includes selected children; excluded children get "ignore" |
| IT-AD-05 | Mixed workflow: some resources adopt, some create new, some ignore | Each category generates appropriate output (import blocks, YAML entries, or nothing) |
| IT-AD-06 | Target-only adoption: adopt target resource with no source match | YAML generated from target data via `normalize_target_fetch()`; import block generated with correct address and target ID |
| IT-AD-07 | Mixed source-matched and target-only adoption in same project | Both source-matched and target-only adoptions in same project produce valid YAML and import blocks; no conflicts |

#### E2E / Browser Tests

| Test ID | Test Case | Expected Result |
|---------|-----------|-----------------|
| E2E-AD-01 | Match page: set resource to "adopt" and verify grid indicator | Adopt badge/icon visible in grid row |
| E2E-AD-02 | Match page: bulk adopt all matched resources | All matched rows change to "adopt" action |
| E2E-AD-03 | Match page: adopt a job, verify dependency cascade dialog | Dialog shows environment and project as required dependencies |
| E2E-AD-04 | Match page: "Select Whole Project" dialog shows child counts | Dialog lists resource types with counts; checkboxes work |
| E2E-AD-05 | Match page: adopt summary card shows correct counts | Summary reflects current adoption, creation, and ignore counts |
| E2E-AD-06 | Deploy page: import count shown in summary | "N resources to import" displayed correctly |
| E2E-AD-07 | Deploy page: `terraform plan` shows imports | Plan output contains "will be imported" for adopted resources |
| E2E-AD-08 | Match page: target-only resources visible with "Target Only" badge | Target-only rows appear in grid with badge and empty Source column |
| E2E-AD-09 | Match page: toggle "Show Target-Only Resources" hides/shows target-only rows | Filter toggle controls visibility of target-only rows; source-matched rows always visible |
| E2E-AD-10 | Match page: adopt a target-only resource | Changing action to "adopt" on a target-only row works; dependency dialog fires if needed |
| E2E-AD-11 | Match page: "Adopt All Target-Only" bulk action | All target-only rows change to "adopt" action |
| E2E-AD-12 | Match page: first-run dialog appears when unmatched target-only resources exist | Dialog shows count and offers "Yes, show them" / "No thanks" / "Remember this choice" |
| E2E-AD-13 | Match page: first-run dialog does NOT appear when all targets are matched | No dialog when no target-only resources exist |
| E2E-AD-14 | Match page: "Remember this choice" persists preference across sessions | After checking "Remember" and dismissing, reloading the page does not show the dialog again |
| E2E-AD-15 | Configure page: target-only adoption toggle changes project default | Changing toggle in Configure updates the project-level preference |
| E2E-AD-16 | Match page: "Show Scoped Only" toggle hides state-only and target-only rows | Toggling filter ON removes state-only and target-only rows; source-scoped rows remain |
| E2E-AD-17 | Match page: scope filter + type filter work together | Both filters active simultaneously; rows must pass both to be visible |
| E2E-AD-18 | Match page: scope filter does not change row actions | Hiding and re-showing rows via filter preserves all actions and dispositions |

##### YAML Generation E2E

| Test ID | Test Case | Expected Result |
|---------|-----------|-----------------|
| E2E-YG-01 | Match page: adopt resource, navigate to Deploy, verify YAML file has target values | `dbt-cloud-config.yml` contains adopted resource with target IDs and names |
| E2E-YG-02 | Match page: adopt resource with protection, verify YAML has `protected: true` | YAML entry for adopted resource includes `protected: true` |
| E2E-YG-03 | Match page: adopt a target-only resource, verify YAML generated from target data | YAML entry produced via `normalize_target_fetch()` with correct target values (no source merge) |
| E2E-YG-04 | Match page: regenerate YAML after adoption, verify adoption state preserved | Navigating back to Match and regenerating YAML retains adoption overrides |

##### TF State Awareness E2E

| Test ID | Test Case | Expected Result |
|---------|-----------|-----------------|
| E2E-TS-01 | Match page: load with TF state fixtures, verify "Already Managed" badge on state-present resources | Resources present in mock `terraform.tfstate` display "Already Managed" badge in grid |
| E2E-TS-02 | Match page: verify state address shown in grid for already-managed resources | Grid column shows TF address (e.g., `module.dbt_cloud.dbtcloud_job.jobs["my_job"]`) |
| E2E-TS-03 | Deploy page: generate import blocks, verify already-managed resources excluded from output | `adopt_imports.tf` does not contain `import {}` blocks for resources already in TF state |

##### Protection E2E

| Test ID | Test Case | Expected Result |
|---------|-----------|-----------------|
| E2E-PR-01 | Match page: mark adopted resource as protected in grid, verify checkbox state persists | Protection checkbox checked; refreshing or re-snapshotting shows checkbox still checked |
| E2E-PR-02 | Match page: protect adopted job, verify cascade dialog (environment + project) | Cascade dialog lists environment and project as suggested protection targets |
| E2E-PR-03 | Match page: navigate away and back, verify protection decisions persisted | After navigating to Configure and back to Match, protection checkboxes retain their state |
| E2E-PR-04 | Deploy page: verify protected address in generated import block file | `adopt_imports.tf` uses `protected_<type>` addresses for protected resources |

##### Full End-to-End Workflow E2E

| Test ID | Test Case | Expected Result |
|---------|-----------|-----------------|
| E2E-FLOW-01 | Source-matched adopt: Match page adopt → Configure → Deploy → verify YAML + import blocks + summary counts | Full flow produces valid YAML, import blocks, and correct deploy summary |
| E2E-FLOW-02 | Target-only adopt: show target-only → adopt → Deploy → verify YAML + import blocks | Target-only adoption end-to-end produces YAML from target data and import blocks |
| E2E-FLOW-03 | Mixed flow: some adopt, some create, some ignore → verify each category in output | YAML has correct entries per action; import blocks only for adopted; summary counts match |
| E2E-FLOW-04 | Protected adopt flow: adopt with protection → Deploy → verify protected addresses everywhere | Protected adopted resources have `protected: true` in YAML, `protected_<type>` in import blocks, and `prevent_destroy` in TF output |

#### Manual Testing Checklist

- [ ] Open Match page and set a resource to "adopt" — verify grid shows adoption indicator
- [ ] Use "Adopt All Matched" — verify all matched rows update
- [ ] Adopt a job — verify dependency dialog appears for environment and project
- [ ] Click "Adopt All" in dependency dialog — verify parents also adopted
- [ ] Click "Skip Dependencies" — verify only child is adopted
- [ ] Use "Select Whole Project" — verify dialog with child counts appears
- [ ] Customize project selection — verify excluded children are set to "ignore"
- [ ] Navigate to Deploy page — verify import count in summary
- [ ] Generate import blocks — verify `adopt_imports.tf` contains correct blocks
- [ ] Preview import blocks — verify content is shown before writing
- [ ] Run `terraform plan` — verify "will be imported" for adopted resources
- [ ] Run `terraform apply` — verify resources imported into state
- [ ] Verify already-managed resources show correct status in grid
- [ ] Adopt a resource with protection — verify protected address in import block
- [ ] Change protection status — verify moved block generated
- [ ] Verify target-only resources appear in grid with "Target Only" badge
- [ ] Toggle "Show Target-Only Resources" — verify rows hide/show
- [ ] Adopt a target-only resource — verify YAML generated from target data
- [ ] Adopt a target-only job — verify dependency dialog fires for environment/project
- [ ] Use "Adopt All Target-Only" — verify all target-only rows update
- [ ] Mixed adoption (some source-matched, some target-only) — verify both produce valid output
- [ ] Adopt a target-only resource with protection — verify protected import block
- [ ] First Match page load with unmatched targets — verify first-run dialog appears
- [ ] Click "No thanks" in dialog — verify target-only rows hidden, toggle OFF
- [ ] Click "Yes, show them" in dialog — verify target-only rows visible, toggle ON
- [ ] Check "Remember this choice" + dismiss — verify dialog does not reappear on reload
- [ ] Configure page — verify target-only adoption toggle present and changes project default
- [ ] Override preference via grid toggle — verify does not change project default
- [ ] Match page with no unmatched targets — verify dialog does NOT appear
- [ ] Set preference to "No" — verify source-to-target matching still works on source-driven rows
- [ ] Toggle "Show Scoped Only" ON — verify state-only rows hidden
- [ ] Toggle "Show Scoped Only" ON — verify target-only rows hidden
- [ ] Toggle "Show Scoped Only" ON — verify source-scoped rows and their matches still visible
- [ ] Toggle "Show Scoped Only" OFF — verify all rows reappear with unchanged actions
- [ ] Combine "Show Scoped Only" + type filter — verify both work together
- [ ] With scope filter ON, check summary card shows filtered counts with "(filtered)" note

---

### Test Fixtures

Each implementation phase requires specific mock data. Fixtures can live in `tests/fixtures/` as JSON files or be constructed inline in test modules. For E2E/browsermcp tests, fixtures must be loadable via the web UI (e.g., placing files in expected paths before starting the server).

#### Source Fixtures

- **File:** `tests/fixtures/source_report_items.json`
- **Contents:** Mock `report_items` JSON with **5 projects** (e.g., Analytics, Marketing, Finance, Engineering, Data-Ops), each containing 2-3 environments, 2-4 jobs, 1 repository, and a few env variables
- **Purpose:** Drives the source side of the Match grid; 3 of 5 projects should overlap with target fixtures

#### Target Fixtures

- **File:** `tests/fixtures/target_fetch_data.json`
- **Contents:** Mock target fetch JSON with **5 projects** — 3 overlap with source fixtures (Analytics, Marketing, Finance) plus 2 target-only projects (Platform-Infra, Legacy-Warehouse)
- **Purpose:** Provides target resources for matching and target-only adoption scenarios; target-only projects have environments, jobs, and repositories with no source equivalent

#### TF State Fixtures

- **File:** `tests/fixtures/terraform.tfstate`
- **Contents:** Mock `terraform.tfstate` with ~5 resources already managed — mix of protected and unprotected, spanning at least 2 projects (e.g., `protected_jobs["analytics_daily"]`, `dbtcloud_environment.environments["marketing_prod"]`)
- **Purpose:** Validates "Already Managed" badge display, import block exclusion, and protection address mismatch detection (UT-AD-07)

#### SelectionManager Fixtures

- **File:** `tests/fixtures/selection_scope.json`
- **Contents:** Scope selection including 3 of 5 source projects (Analytics, Marketing, Finance) — used by the "Show Scoped Only" filter
- **Purpose:** Tests scope visibility filter (US-SV-01 through US-SV-07); unselected source projects should be hidden when filter is ON

#### Protection Intent Fixtures

- **File:** `tests/fixtures/protection-intent.json`
- **Contents:** Mock protection intent with some pre-protected resources (e.g., `analytics_daily` job protected, `marketing_prod` environment protected)
- **Purpose:** Validates protection persistence across navigation (E2E-PR-03), cascade dialog (E2E-PR-02), and protection address in import blocks (E2E-PR-04)

#### Fixture Coverage Matrix

| Phase | Source | Target | TF State | Scope | Protection |
|-------|--------|--------|----------|-------|------------|
| 1a: Core Adopt | required | required | — | — | — |
| 1b: Target-Only | required | required (with target-only projects) | — | — | — |
| 1c: TF State | required | required | required | — | — |
| 1d: Dependency | required | required | — | — | — |
| 1e: Protection | required | required | — | — | required |
| 1f: Deploy | required | required | required | — | required |
| Scope Filter | required | required | required | required | — |
| Preference | required | required (with target-only) | — | — | — |

---

### Acceptance Criteria

#### Must Have

- [ ] Adopt action available for source-matched resources in the Match grid
- [ ] Target-only resources (no source match) visible in the Match grid with "Target Only" badge
- [ ] Target-only resources default to "Ignore"; user can change to "Adopt"
- [ ] YAML generation for source-matched adoptions uses target overrides (`adoption_yaml_updater.py`)
- [ ] YAML generation for target-only adoptions uses target data (`normalize_target_fetch()`)
- [ ] TF 1.5+ `import {}` blocks generated for all adopted resources (both source-matched and target-only)
- [ ] Import blocks use correct TF resource addresses (including protected variants)
- [ ] All supported resource types handled: PRJ, ENV, JOB, REP, PREP, EXTATTR, VAR
- [ ] TF state cross-referencing identifies already-managed resources
- [ ] Already-managed resources excluded from import block generation
- [ ] Protection flag (`protected: true`) flows through from adoption to YAML to import blocks

#### Should Have

- [ ] "Show Scoped Only" visibility filter in grid toolbar (hides state-only and target-only rows unrelated to source scope; pure UI, no logic impact)
- [ ] "Show Target-Only Resources" filter toggle in grid toolbar (default driven by preference)
- [ ] Smart first-run dialog on Match page when unmatched target-only resources exist
- [ ] Project-level preference for target-only visibility (persists across sessions)
- [ ] "Remember this choice" option in first-run dialog
- [ ] Configure page toggle for target-only adoption preference
- [ ] Dependency cascade dialog when adopting child resources (both source-matched and target-only)
- [ ] "Select Whole Project" with child count dialog and customization
- [ ] Bulk actions: "Adopt All Matched", "Adopt All Target-Only", "Ignore All"
- [ ] Adoption summary card showing counts by action type (including target-only vs. source-matched)
- [ ] Import block preview before writing to disk
- [ ] Deploy page import count in summary
- [ ] Protection status change generates `moved` blocks

#### Nice to Have

- [ ] Import block cleanup after successful apply
- [ ] Legacy CLI `terraform import` fallback for TF < 1.5
- [ ] Visual distinction between source-matched and target-only adoptions in the deploy summary

---

### Implementation Phases (Part 1)

#### Phase 1a: Core Adopt Flow (Source-Matched)

- Adopt action option in Match grid for source-matched resources
- `apply_adoption_overrides()` coverage for all resource types
- Import block generation via `generate_adopt_imports_from_grid()`
- Unit tests: UT-AD-01 through UT-AD-05
- **Done when:** Adopting a source-matched resource produces valid YAML and import blocks for PRJ, ENV, JOB, REP, PREP, EXTATTR, VAR

#### Phase 1b: Target-Only Adoption

- Surface target-only resources (no source match) in the Match grid
- "Target Only" badge and empty Source column for target-only rows
- Default action = "Ignore"; user changes to "Adopt"
- "Show Target-Only Resources" filter toggle
- "Adopt All Target-Only" bulk action
- YAML generation for target-only adoptions via `normalize_target_fetch()`
- Import block generation for target-only adoptions
- Unit tests: UT-AD-11 through UT-AD-13
- Integration tests: IT-AD-06, IT-AD-07
- E2E tests: E2E-AD-08 through E2E-AD-11
- **Depends on:** Phase 1a
- **Done when:** Target-only resources appear in grid; adopting them produces valid YAML and import blocks; filter toggle works

#### Phase 1c: TF State Awareness

- Cross-reference target resources with `terraform.tfstate`
- "Already Managed" status in grid for state-present resources
- Exclude already-managed from import block generation
- Unit tests: UT-AD-06, UT-AD-07
- **Depends on:** Phases 1a, 1b
- **Done when:** Resources in TF state show "Already Managed" and are not double-imported (for both source-matched and target-only rows)

#### Phase 1d: Dependency Handling & Project Selection

- Cascade dialog for child resource adoption (uses `hierarchy_index.py`)
- Dependencies can cross the source-matched / target-only boundary
- "Select Whole Project" with child count dialog
- Bulk adopt/ignore actions
- Integration tests: IT-AD-04
- E2E tests: E2E-AD-03, E2E-AD-04
- **Depends on:** Phases 1a, 1b
- **Done when:** Adopting a job triggers dependency dialog; dependencies work across source-matched and target-only rows; "Select Whole Project" works with customization

#### Phase 1e: Protection Integration

- Protection checkbox for adopted resources (both source-matched and target-only)
- Protected addresses in import blocks
- Moved block generation for protection status changes
- Unit tests: UT-AD-02, UT-AD-04
- Integration tests: IT-AD-03
- **Depends on:** Phase 1a
- **Done when:** Protected adoptions produce correct import blocks and moved blocks

#### Phase 1f: Deploy Integration

- Import count in deploy summary (distinguishes source-matched vs. target-only if useful)
- `terraform plan` shows imports
- Import block cleanup after apply
- E2E tests: E2E-AD-06, E2E-AD-07
- **Depends on:** Phases 1a, 1b, 1c, 1e
- **Done when:** Full adoption → deploy → import flow works end-to-end for both source-matched and target-only resources

---

### Ralph Wiggum Tasking

This section maps implementation phases to RALPH_TASK.md criteria. Each criterion is independently testable and committable. Unit test criteria come first (fast feedback loop), followed by E2E criteria (require running server + browsermcp).

#### Suggested Criterion Ordering

1. **Unit tests first** — fast, no server required, validates core logic
2. **Integration tests** — validates cross-module interactions
3. **UI implementation** — build the UI components (grid changes, dialogs, toggles)
4. **E2E tests** — validates full browser flows with browsermcp (requires `./restart_web.sh`)

#### Phase 1a: Core Adopt Flow — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 1 | `generate_adopt_imports_from_grid()` produces import blocks for adopt rows and skips ignore rows | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_01 or UT_AD_25"` | false |
| 2 | `generate_adopt_imports_from_grid()` uses `protected_<type>` addresses for protected rows | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_02"` | false |
| 3 | `apply_adoption_overrides()` overwrites YAML with target values and preserves protection flag | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_03 or UT_AD_04"` | false |
| 4 | Import block addresses resolve correctly for all 7 resource types (PRJ, ENV, JOB, REP, PREP, EXTATTR, VAR) | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_05"` | false |
| 5 | Match grid shows "Adopt Existing" action for source-matched resources; adopt badge visible | browsermcp: navigate to Match, set resource to adopt, snapshot grid | true |
| 6 | "Adopt All Matched" and "Ignore All Unmatched" bulk actions work in grid | browsermcp: click bulk buttons, verify row actions update | true |
| 7 | Adoption summary card shows correct counts (adopt matched, create, ignore) | browsermcp: verify summary card after adopt actions | true |

#### Phase 1b: Target-Only Adoption — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 8 | `build_grid_data()` produces `is_target_only: True` with default action="ignore" and empty source columns | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_20 or UT_AD_21 or UT_AD_22"` | false |
| 9 | `normalize_target_fetch()` generates valid YAML for target-only resources | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_11"` | false |
| 10 | `generate_adopt_imports_from_grid()` handles mixed source-matched and target-only rows | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_12"` | false |
| 11 | Target-only rows visible in grid with "Target Only" badge and empty Source column | browsermcp: navigate to Match, snapshot, verify badge and layout | true |
| 12 | "Show Target-Only Resources" toggle hides/shows target-only rows; "Adopt All Target-Only" bulk action works | browsermcp: toggle filter, verify visibility; click bulk adopt | true |

#### Phase 1b-ext: Target-Only Preference — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 13 | Project-level preference loads from config and drives grid toggle default | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_14"` | false |
| 14 | First-run dialog triggers only when unmatched target-only resources exist | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_15"` | false |
| 15 | First-run dialog: "Yes, show them" / "No thanks" / "Remember this choice" all work | browsermcp: trigger dialog, test each button, verify toggle and preference persistence | true |
| 16 | Configure page toggle changes project-level preference | browsermcp: navigate to Configure, change toggle, verify persistence | true |

#### Phase 1b-ext: Scope Visibility Filter — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 17 | Scope filter hides state-only and target-only rows when ON; preserves source-scoped rows | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_16 or UT_AD_17 or UT_AD_18"` | false |
| 18 | Scope filter does not alter row actions or dispositions | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_19"` | false |
| 19 | "Show Scoped Only" toggle in grid toolbar works and composes with type filter | browsermcp: toggle scope filter, verify rows; combine with type filter | true |
| 20 | Summary card updates to show filtered counts with "(filtered — N rows hidden)" note | browsermcp: activate scope filter, verify summary card text | true |

#### Phase 1c: TF State Awareness — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 21 | State cross-reference identifies already-managed resources (including protection mismatch) | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_06 or UT_AD_07"` | false |
| 22 | "Already Managed" badge and TF state address shown in grid for state-present resources | browsermcp: load Match with TF state fixtures, verify badges and addresses | true |
| 23 | Already-managed resources excluded from import block output | browsermcp: generate import blocks, verify exclusion in output file | true |

#### Phase 1d: Dependency Handling — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 24 | Dependency resolution returns correct parent chain for all child types | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_08 or UT_AD_24"` | false |
| 25 | Cascade dialog shows when adopting child without parent; "Adopt All" and "Skip" buttons work | browsermcp: adopt a job, verify dialog, test both buttons | true |
| 26 | "Select Whole Project" dialog shows child counts and supports checkbox customization | browsermcp: trigger project dialog, verify counts, customize selection | true |

#### Phase 1e: Protection Integration — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 27 | Protection checkbox available for adopted resources; cascade dialog suggests parent protection | browsermcp: check protection on adopted job, verify cascade dialog | true |
| 28 | Protection decisions persist across page navigation (Match → Configure → Match) | browsermcp: set protection, navigate away, navigate back, verify state | true |
| 29 | Protected addresses in import blocks and moved blocks for protection status changes | `pytest importer/web/tests/test_adoption*.py -v -k "UT_AD_02 or UT_AD_04"` + verify file output | false |

#### Phase 1f: Deploy Integration — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 30 | Deploy summary shows import count (N to import, M to create, K protected) | browsermcp: navigate to Deploy, verify summary card | true |
| 31 | `terraform plan` shows "will be imported" for adopted resources | browsermcp: run plan from Deploy page, verify output | true |
| 32 | `terraform apply` imports adopted resources into state with correct target IDs | browsermcp: run apply, verify success message | true |
| 33 | Import block cleanup after successful apply | `pytest importer/web/tests/test_adoption*.py -v -k "cleanup"` or verify file removal after apply | false |

#### Full Workflow E2E — RALPH_TASK Criteria

| # | Criterion | test_command | browser_validation |
|---|-----------|-------------|-------------------|
| 34 | Source-matched adopt end-to-end: Match adopt → Configure → Deploy → verify YAML + imports + summary | browsermcp: full flow (E2E-FLOW-01) | true |
| 35 | Target-only adopt end-to-end: show target-only → adopt → Deploy → verify YAML + imports | browsermcp: full flow (E2E-FLOW-02) | true |
| 36 | Mixed flow end-to-end: adopt + create + ignore → verify each category in output | browsermcp: full flow (E2E-FLOW-03) | true |
| 37 | Protected adopt end-to-end: adopt with protection → Deploy → verify everywhere | browsermcp: full flow (E2E-FLOW-04) | true |

#### Fixture Setup Instructions

Before starting browsermcp E2E criteria:

1. **Place fixture files** in expected paths:
 - Copy `tests/fixtures/source_report_items.json` to the session's source data path
 - Copy `tests/fixtures/target_fetch_data.json` to the session's target data path
 - Copy `tests/fixtures/terraform.tfstate` to the deployment directory (for Phase 1c+)
 - Copy `tests/fixtures/selection_scope.json` to the selection manager path (for scope filter tests)
 - Copy `tests/fixtures/protection-intent.json` to the protection intent path (for Phase 1e+)

2. **Start the server**: `./restart_web.sh`

3. **Load credentials**: Navigate to `/fetch_target`, click "Load .env", handle any existing data dialog

4. **Navigate to test page**: Use browsermcp `browser_navigate` to the target page

#### Criterion Numbering Summary

| Criteria | Phase | Type | Count |
|----------|-------|------|-------|
| 1–4 | 1a: Core Adopt | unit | 4 |
| 5–7 | 1a: Core Adopt | E2E | 3 |
| 8–10 | 1b: Target-Only | unit | 3 |
| 11–12 | 1b: Target-Only | E2E | 2 |
| 13–14 | 1b-ext: Preference | unit | 2 |
| 15–16 | 1b-ext: Preference | E2E | 2 |
| 17–18 | 1b-ext: Scope Filter | unit | 2 |
| 19–20 | 1b-ext: Scope Filter | E2E | 2 |
| 21 | 1c: TF State | unit | 1 |
| 22–23 | 1c: TF State | E2E | 2 |
| 24 | 1d: Dependency | unit | 1 |
| 25–26 | 1d: Dependency | E2E | 2 |
| 27–28 | 1e: Protection | E2E | 2 |
| 29 | 1e: Protection | unit | 1 |
| 30–32 | 1f: Deploy | E2E | 3 |
| 33 | 1f: Deploy | unit | 1 |
| 34–37 | Full Workflow | E2E | 4 |
| **Total** | | | **37** |

---

## Part 2: Import & Adopt Workflow (Future)

### Vision

A standalone workflow that works purely against a single target dbt Cloud account — no source account needed. Users fetch their target account, review what exists, select which resources to bring under Terraform management, and deploy.

### Key Differences from Migration

| Aspect | Migration Workflow | Import & Adopt |
|--------|-------------------|----------------|
| Source account | Required | Not needed |
| Driver | Source items drive grid; target matches | Target items drive grid |
| Default action | Match (adopt existing) or Create New | Adopt (import to TF) or Ignore |
| YAML generation | Source YAML normalized, then target overrides applied | Target fetch directly normalized to config via `normalize_target_fetch()` |
| Import blocks | Generated for matched/adopted resources | Generated for all adopted resources |
| State-only items | Inherited from prior apply | Not applicable (fresh start) |

### Reusability Map: Part 1 Components in Part 2

Part 1's target-only adoption code path (US-TO-01 through US-TO-10) is the **direct precursor** to Part 2. When the Import & Adopt workflow runs, _every_ resource is "target-only" — there is no source. The same grid rendering, YAML generation, import block creation, and dependency cascade logic applies.

| Part 1 Component | Part 2 Reuse | Adaptation Needed |
|-------------------|-------------|-------------------|
| `build_grid_data()` in `match_grid.py` | Drives the `/adopt` page grid | Part 2 = all rows are "target-only" mode from Part 1; remove Source column entirely; default action = "adopt" instead of "ignore" |
| Target-only YAML generation via `normalize_target_fetch()` | Generates all YAML in Part 2 | No changes — Part 1 target-only adoption already exercises this exact code path |
| `terraform_import.py` | Generates import blocks for all adopted resources | No changes — already workflow-agnostic |
| `adoption_yaml_updater.py` | Applies target values to YAML | No changes — already works on target data |
| `ProtectionIntentManager` in `protection_intent.py` | Tracks protection for adopted resources | No changes — already workflow-agnostic |
| Cascade dependency dialogs (Part 1 US-DH-01 through US-DH-03) | Reused for dependency-assist in adopt grid | No changes — same dialog, same hierarchy_index lookups |
| `hierarchy_index.py` | Dependency resolution for adopt cascades | No changes |
| `target-intent.json` with `adopt`/`ignore` dispositions (AD-3) | Intent file for the adopt workflow | Same format; workflow type distinguishes migration vs. standalone |
| "Target Only" badge and filter toggle (Part 1 AD-5) | Not needed in Part 2 — all rows are target resources | Toggle removed; badge removed; grid is purely target-driven |

### Three Resource Categories

1. **Target resources** — exist in the target dbt Cloud account. User decides: **Adopt** (bring under TF management) or **Ignore** (leave unmanaged).

2. **State resources** (Day 2 scenario) — resources already in `terraform.tfstate` from a prior partial adoption. Shown as "Already Managed" and auto-included.

3. **Global resources** — account-level resources (groups, service tokens, etc.) follow patterns from [42.01 Part 2](42.01-Global-Resources-Target-Intent.md) (future work — dependency acknowledged).

### Proposed Steps

```
1. Fetch Target          — reuse existing /fetch_target page (no source needed)
2. Explore Target        — reuse existing /explore_target page
3. Select & Adopt        — NEW page: /adopt (target-driven grid, reuses match_grid patterns)
4. Configure             — reuse existing /configure page
5. Target Credentials    — reuse existing /target_credentials page
6. Deploy                — reuse existing /deploy page (with import block generation)
```

### The `/adopt` Page (Future)

Reuses Part 1's `build_grid_data()` and grid patterns with target-only configuration:

#### Grid Columns

| Column | Type | Notes |
|--------|------|-------|
| Type | Badge | Resource type code (PRJ, ENV, JOB, etc.) with icon and color |
| Name | Text | Target resource name |
| Target ID | Number | dbt Cloud resource ID |
| Action | Dropdown | Adopt / Ignore / Already Managed (read-only) |
| Protected | Checkbox | Marks for `lifecycle { prevent_destroy = true }` |
| State Address | Text | TF state address if already managed (otherwise empty) |
| Project | Text | Parent project name for project-scoped resources |

#### Actions Per Row

- **Adopt** (default for unmanaged) — generates YAML entry + import block
- **Ignore** — leave unmanaged, no TF config generated
- **Already Managed** (read-only) — resource exists in TF state already

### Future Work Items

| Item | Details |
|------|---------|
| New `WorkflowStep.ADOPT` enum value | Add to `importer/web/state.py` |
| New `WORKFLOW_STEPS[IMPORT_ADOPT]` | Replace current source-including steps with: `[FETCH_TARGET, EXPLORE_TARGET, ADOPT, CONFIGURE, TARGET_CREDENTIALS, DEPLOY]` |
| New `/adopt` page | `importer/web/pages/adopt.py` — reuses `build_grid_data()` patterns from `match_grid.py` |
| Enable home page card | Remove `disabled=True` and "Coming Soon" badge from `home.py` |
| Stepper integration | Add Import & Adopt to workflow dropdown in `stepper.py`; remove static "Coming Soon" label |
| Route registration | Register `/adopt` route in `importer/web/app.py` |

### Future Open Questions

1. **Global resources in adopt grid?** Depends on [42.01 Part 2](42.01-Global-Resources-Target-Intent.md) (future). When implemented, global resources would appear in the adopt grid alongside project-scoped resources.
2. **Day 2 reconciliation?** After initial adoption, how does the user add newly created target resources to TF management? Re-run the workflow, or incremental adopt from Deploy page?
3. **Partial adoption and drift?** If user adopts some resources but ignores others, how do we handle drift between TF-managed and unmanaged resources that reference each other?

---

## Governance

This PRD is subject to the **Workflow Governance** rules in
[0.17 of 00.01-Standards-of-Development.md](00.01-Standards-of-Development.md#017-workflow-governance--reuse-first--test-gates):

- Adoption code must use the canonical contracts (generate pipeline,
  terraform helpers, reconcile source).
- Changes require passing `test_contract_enforcement.py` and
  `test_cross_page_pipeline_consistency.py`.

---

## Navigation

- [43.02-Adoption-Terraform-Step.md](43.02-Adoption-Terraform-Step.md) — Dedicated adoption step (continuation)
- [43.03-Unified-Protect-Adopt-Pipeline.md](43.03-Unified-Protect-Adopt-Pipeline.md) — **Unified Protect & Adopt Pipeline** (consolidates adoption + protection execution into headless pipeline)
- [42.01-Global-Resources-Target-Intent.md](42.01-Global-Resources-Target-Intent.md) — Global resource handling patterns
- [41.02-Adding-New-Terraform-Object-Support.md](41.02-Adding-New-Terraform-Object-Support.md) — Resource type support checklist
- [31.02-Resource-Protection.md](31.02-Resource-Protection.md) — Protection system architecture
- [docs/architecture/canonical-contracts.md](../docs/architecture/canonical-contracts.md) — Canonical workflow contracts
- [00.01-Standards-of-Development.md](00.01-Standards-of-Development.md) — Development standards and PRD index
